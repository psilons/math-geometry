

% Preamble
\documentclass{article}

% Packages
\usepackage{amsmath}

% Document
\begin{document}
    This is latex logo, \LaTeX % print latex
% special characters need to be escaped by \, such as # $ & _ { }
% font shapes: \textbf, \textit, \textsl, \textsc, \emph,
%colors
% paragraphs - text blocks(position), textbox(grouping), font, spacing, color, and other styles
    % including font size, style, foreground/background colors
% fonts: \textsf - serifs font
 %      \textfm - Roman font
    % page break
\end{document}
